# 1.1
float32 в питоне я получу, импортировав numpy.

Сначала захотелось делить числа на 2, пока последнее не будет равно предпоследнему. тогда разница между предпредпоследним и последним была бы тем Эпсилон. Это не сработало, т.к. я просто получил субнормальные числа и пришел к равенству 0=0, буду считать по определению.

1+ $\epsilon/2$  = 1
1+ $\epsilon$ = чуть больше 1..
(1 + ϵ) + ϵ/2 = 1 + ϵ

ϵ = b^(1−p), где p - значащие цифры - это все цифры до последнего корректного. p = log_b()

Оказывается, в условии было написано  ϵ = nextafter(1, +inf) - 1... Проверил, е обоими способами получил $\epsilon= 1.1920929e{-07}$ для float32

(1 +  1e-16) +  1e-16 = 1.0
1 + ( 1e-16 +  1e-16) = 1.0000000000000002

В первом случае такое округление получилось, т.к. 1e-16 < $\epsilon/2$  (1+ 1e-16 = 1).
Во втором случае 2e-16 > $\epsilon/2$, поэтому результат отличается

Теперь хочу получить E_max. Просто умножаю на 2 пока число не перестанет увеличиваться..

find_E_min делю, пока число не округляется до самого себя при умножении на (1+eps).

# 1.2
Получение матеактической оценки усечения:
пока не разобрался. В коде проверил, верно.

вывел суммы для 3-х алгоритмов (отдельный файлик в папке)
Вот что я заметил: раздельное складывание - самый неточный способ подсчета.  Самым точным оказался результат при суммировании в обратном порядке для 1e4. При этом в прямом порядке отличие в  1e-6 (6-цифре после запятой). 

Я не понял что нужно сделать в 4 вопросе. Ну пусть S с крышечкой - это float32 (я выбрал его изначально), без - 64. 

Вывел график. 
	 Ошибка усечения преобладает (вносит больший вклад в общую ошибку) примерно до 1e5. Самые крутые результаты показал алгоритм сложения справа налево. У него при больших значениях n и усечение и ошибка вычислений(округление) меньше чем у других алгоритмов.Интересно, что у прямой суммы график соединился на 1e6. СЛожение раздельно дает совсем неадекватные результаты при больших n.

__
	Порядок влияет на ответ, потому что на сумму влияет то, числа каких размеров мы складываем. Если сложить большое с меньшем, то точность будет меньше, потому что в мандиссе может не хватить знаков для маленького числа. (сложение вещественных чисел неассоциативно)
__
	Раздельное сложение положительных и отрицательных чисел плохо тем, что
	1) мы получаем очень большие по модулю числа, разница между которыми очень мала (тут меньше 1). 
	2) накапливается ошибка при сложении от большего к меньшему
__
	Самым лучшим алгоритмом оказалось суммирование обратно (справа налево).